• Upon successful user input validation, INSERT a record into the
 “registration” table before going to the confirmation page.

o Do NOT pass registration data directly to the confirmation page
 for this project

o Allowed to pass the registration data primary key to the
 confirmation page

o Default MySQL username “root” with password “” (yes, that is an
 empty string password). Use this to connect to the MySQL database,
 if possible. Some installations might require an actual password.

• When loading the confirmation page, SELECT this data record from
 the “registration” table then display this data on the
  confirmation page.

• coding standards http://www.w3schools.com/html/html5_syntax.asp